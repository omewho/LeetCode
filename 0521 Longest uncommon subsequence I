class Solution {
public:
    int solve(string a, string b) {
        if(a == b) return -1;
        return max(a.length(), b.length());
    }

    int findLUSlength(string a, string b) {
        return solve(a, b);
    }
};
