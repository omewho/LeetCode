class Solution {
public:
    int countNegatives(vector<vector<int>>& grid) {
        int ans = 0;
        for(auto g: grid) {
            for(int num: g) {
                if(num < 0) ans++;
            }
        }
        return ans;
    }
};
